import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    width: '100%',
    backgroundColor: '#4396f4',
    borderRadius: 25,
    alignItems: 'center',
    paddingVertical: 14,
  },
  buttonText: {
    color: '#fff',
    fontSize: 16,
  },
});
