import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    width: '100%',
    backgroundColor: '#fff',
    borderRadius: 25,
    paddingHorizontal: 16,
    height: 50,
    marginBottom: 16,
  },
});
