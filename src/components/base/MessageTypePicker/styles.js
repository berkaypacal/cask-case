import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  dropdown: {
    width: '100%',
    backgroundColor: '#fff',
    borderRadius: 25,
    marginBottom: 16,
    overflow: 'hidden',
  },
  picker: {
    height: 50,
    width: '100%',
  },
});
