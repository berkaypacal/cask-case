import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  image: {
    width: '100%',
    height: 400,
    borderRadius: 30,
  },
  fallbackContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 400,
  },
  fallbackText: {
    fontSize: 16,
    color: '#888',
  },
});
